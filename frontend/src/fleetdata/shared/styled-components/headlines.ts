import styled from 'styled-components';
import { fontXXL, lightblue } from '../styles/variables';

export const StyledHeadline = styled.h1`
  color: ${lightblue};
  margin-top: 0;
  font-size: ${fontXXL};
  margin-bottom: 1rem;
`;
